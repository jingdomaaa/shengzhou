<block wx:if="{{$taroCompReady}}">
    <view class="{{anonymousState__temp}}" style="{{anonymousState__temp2}}">
        <view wx:key="$original.value" bindtap="handleClick" class="{{option.$loopState__temp4}}" wx:for="{{loopArray1}}" wx:for-item="option" wx:for-index="_anonIdx2" data-e-tap-so="this" data-e-tap-a-a="{{option.$original}}">
            <view class="at-radio__option-wrap">
                <view class="at-radio__option-container">
                    <view class="at-radio__title">{{option.$original.label}}</view>
                    <view class="{{option.$loopState__temp6}}"><text class="at-icon at-icon-check"></text>
                    </view>
                </view>
                <block wx:if="{{option.$original.desc}}">
                    <view class="at-radio__desc">{{option.$original.desc}}</view>
                </block>
            </view>
        </view>
    </view>
</block>